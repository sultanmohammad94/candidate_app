version: "3"
services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile_fastapi
    ports:
      - "8000:8000"
    depends_on:
      - mongo
    env_file:
      - .env
  mongo:
    build:
      context: .
      dockerfile: Dockerfile_mongo
    ports:
      - "27017:27017"
